{
  "name": "skpm-workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "typecheck": "npm run -w apps/cli typecheck",
    "lint:fix": "npm run -w apps/cli lint:fix",
    "test": "npm run -w apps/cli test",
    "build": "npm run -w apps/cli build"
  }
}
